<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assignments - Professor Portal</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        .content {
            padding: 30px;
        }
        .assignments-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        .assignments-table th,
        .assignments-table td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        .assignments-table th {
            background-color: #f8f9fa;
            font-weight: 600;
            color: #333;
        }
        .assignments-table tr:hover {
            background-color: #f8f9fa;
        }
        .status {
            padding: 5px 15px;
            border-radius: 20px;
            font-size: 0.9em;
            font-weight: 500;
            display: inline-block;
        }
        .status.pending {
            background-color: #fff3cd;
            color: #856404;
        }
        .status.submitted {
            background-color: #cfe2ff;
            color: #084298;
        }
        .status.approved {
            background-color: #d1e7dd;
            color: #0f5132;
        }
        .status.rejected {
            background-color: #f8d7da;
            color: #842029;
        }
        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            display: inline-block;
            font-size: 0.9em;
            transition: all 0.3s;
        }
        .btn-review {
            background-color: #007bff;
            color: white;
        }
        .btn-review:hover {
            background-color: #0056b3;
        }
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #666;
        }
        .empty-state h2 {
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📚 Assignment Review Portal</h1>
            <p>Professor Dashboard</p>
        </div>
        <div class="content">
            <h2>Assignments List</h2>
            <% if (assignments && assignments.length > 0) { %>
                <table class="assignments-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Title</th>
                            <th>Student</th>
                            <th>Submitted Date</th>
                            <th>Status</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% assignments.forEach(assignment => { %>
                            <tr>
                                <td><%= assignment.id %></td>
                                <td><strong><%= assignment.title %></strong></td>
                                <td><%= assignment.studentName || assignment.studentEmail %></td>
                                <td><%= new Date(assignment.submittedAt).toLocaleDateString() %></td>
                                <td>
                                    <span class="status <%= assignment.status %>">
                                        <%= assignment.status %>
                                    </span>
                                </td>
                                <td>
                                    <% if (assignment.status === 'submitted' || assignment.status === 'pending') { %>
                                        <a href="/professor/assignments/<%= assignment.id %>/review" class="btn btn-review">
                                            Review
                                        </a>
                                    <% } else if (assignment.status === 'rejected') { %>
                                        <span style="color: #842029; font-weight: 500;">Rejected</span>
                                    <% } else { %>
                                        <span style="color: #0f5132; font-weight: 500;">Approved</span>
                                    <% } %>
                                </td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } else { %>
                <div class="empty-state">
                    <h2>No assignments available</h2>
                    <p>There are no assignments to review at this time.</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
