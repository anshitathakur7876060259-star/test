<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Review Assignment - <%= assignment.title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            max-width: 1400px;
            margin: 0 auto;
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
        }
        .header h1 {
            font-size: 2em;
            margin-bottom: 10px;
        }
        .header a {
            color: white;
            text-decoration: none;
            opacity: 0.9;
        }
        .header a:hover {
            opacity: 1;
        }
        .content {
            padding: 30px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
        }
        .section {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
        }
        .section h2 {
            margin-bottom: 20px;
            color: #333;
            border-bottom: 2px solid #667eea;
            padding-bottom: 10px;
        }
        .info-item {
            margin-bottom: 15px;
        }
        .info-label {
            font-weight: 600;
            color: #555;
            margin-bottom: 5px;
        }
        .info-value {
            color: #333;
        }
        .pdf-viewer {
            width: 100%;
            height: 600px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .file-download {
            display: inline-block;
            padding: 10px 20px;
            background-color: #28a745;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 10px;
        }
        .file-download:hover {
            background-color: #218838;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #333;
        }
        .form-group textarea {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-family: inherit;
            font-size: 1em;
            resize: vertical;
            min-height: 120px;
        }
        .signature-section {
            margin-top: 20px;
        }
        .signature-options {
            display: flex;
            gap: 20px;
            margin-bottom: 15px;
        }
        .signature-option {
            flex: 1;
        }
        .signature-option input[type="radio"] {
            margin-right: 5px;
        }
        .signature-text-input {
            width: 100%;
            padding: 12px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-family: 'Courier New', monospace;
            font-size: 1.1em;
        }
        .signature-upload {
            margin-top: 10px;
        }
        .signature-upload input[type="file"] {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }
        .btn {
            padding: 12px 30px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
            font-weight: 600;
            transition: all 0.3s;
            margin-top: 20px;
        }
        .btn-approve {
            background-color: #28a745;
            color: white;
            width: 100%;
        }
        .btn-approve:hover {
            background-color: #218838;
        }
        .btn-approve:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }
        .btn-reject {
            background-color: #dc3545;
            color: white;
            width: 100%;
        }
        .btn-reject:hover {
            background-color: #c82333;
        }
        .btn-reject:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }
        .button-group {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-top: 20px;
        }
        .full-width {
            grid-column: 1 / -1;
        }
        .otp-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.5);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }
        .otp-modal.active {
            display: flex;
        }
        .otp-modal-content {
            background: white;
            padding: 40px;
            border-radius: 10px;
            max-width: 400px;
            width: 90%;
            text-align: center;
        }
        .otp-modal-content h2 {
            margin-bottom: 20px;
            color: #333;
        }
        .otp-input {
            width: 100%;
            padding: 15px;
            font-size: 1.5em;
            text-align: center;
            border: 2px solid #ddd;
            border-radius: 5px;
            margin: 20px 0;
            letter-spacing: 5px;
        }
        .otp-input:focus {
            outline: none;
            border-color: #667eea;
        }
        .btn-verify {
            background-color: #007bff;
            color: white;
            width: 100%;
        }
        .btn-verify:hover {
            background-color: #0056b3;
        }
        .message {
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        .message.success {
            background-color: #d1e7dd;
            color: #0f5132;
            border: 1px solid #badbcc;
        }
        .message.error {
            background-color: #f8d7da;
            color: #842029;
            border: 1px solid #f5c2c7;
        }
        .message.info {
            background-color: #cfe2ff;
            color: #084298;
            border: 1px solid #b6d4fe;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <a href="/professor/assignments">← Back to Assignments</a>
            <h1>Review Assignment: <%= assignment.title %></h1>
        </div>
        <div class="content">
            <div class="section full-width">
                <h2>Assignment Details</h2>
                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                    <div class="info-item">
                        <div class="info-label">Assignment ID</div>
                        <div class="info-value"><%= assignment.id %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Title</div>
                        <div class="info-value"><strong><%= assignment.title %></strong></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Student</div>
                        <div class="info-value"><%= assignment.studentName || assignment.studentEmail %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Student Email</div>
                        <div class="info-value"><%= assignment.studentEmail %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Submitted Date</div>
                        <div class="info-value"><%= new Date(assignment.submittedAt).toLocaleString() %></div>
                    </div>
                    <div class="info-item">
                        <div class="info-label">Status</div>
                        <div class="info-value">
                            <span style="padding: 5px 15px; background: #cfe2ff; color: #084298; border-radius: 20px;">
                                <%= assignment.status %>
                            </span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="section">
                <h2>File Preview</h2>
                <% if (assignment.fileUrl) { %>
                    <iframe src="<%= assignment.fileUrl %>" class="pdf-viewer"></iframe>
                    <a href="<%= assignment.fileUrl %>" download class="file-download">📥 Download File</a>
                <% } else { %>
                    <p>No file available for preview</p>
                <% } %>
            </div>

            <div class="section">
                <h2>Review & Decision</h2>
                <form id="approvalForm">
                    <div class="form-group">
                        <label for="remarks">Remarks/Feedback</label>
                        <textarea id="remarks" name="remarks" placeholder="Enter your remarks or feedback for the student..." required></textarea>
                    </div>

                    <div class="signature-section">
                        <label style="display: block; margin-bottom: 10px; font-weight: 600;">Digital Signature (Required for Approval)</label>
                        <div class="signature-options">
                            <div class="signature-option">
                                <input type="radio" id="signature-text" name="signatureType" value="text" checked>
                                <label for="signature-text">Text Signature</label>
                            </div>
                            <div class="signature-option">
                                <input type="radio" id="signature-image" name="signatureType" value="image">
                                <label for="signature-image">Upload Image</label>
                            </div>
                        </div>

                        <div id="text-signature-container">
                            <input type="text" id="signature-text-input" class="signature-text-input" 
                                   placeholder="Enter your signature" required>
                        </div>

                        <div id="image-signature-container" style="display: none;">
                            <div class="signature-upload">
                                <input type="file" id="signature-image-input" accept="image/*">
                                <div id="signature-preview" style="margin-top: 10px;"></div>
                            </div>
                        </div>
                    </div>

                    <% if (assignment.status === 'submitted' || assignment.status === 'pending') { %>
                        <div class="button-group">
                            <button type="submit" class="btn btn-approve" id="approveBtn">
                                ✓ Approve Assignment
                            </button>
                            <button type="button" class="btn btn-reject" id="rejectBtn">
                                ✗ Reject Assignment
                            </button>
                        </div>
                    <% } else { %>
                        <div style="padding: 20px; background: #e9ecef; border-radius: 5px; text-align: center;">
                            <p style="margin: 0; font-weight: 600; color: #495057;">
                                This assignment has already been 
                                <% if (assignment.status === 'approved') { %>
                                    <span style="color: #0f5132;">approved</span>
                                <% } else if (assignment.status === 'rejected') { %>
                                    <span style="color: #842029;">rejected</span>
                                <% } else { %>
                                    processed
                                <% } %>.
                            </p>
                        </div>
                    <% } %>
                </form>
            </div>
        </div>
    </div>

    <!-- OTP Verification Modal -->
    <div class="otp-modal" id="otpModal">
        <div class="otp-modal-content">
            <h2>Verify OTP</h2>
            <div id="otpMessage"></div>
            <input type="text" id="otpInput" class="otp-input" placeholder="000000" maxlength="6" pattern="[0-9]{6}">
            <button class="btn btn-verify" id="verifyBtn">Verify & Complete Approval</button>
            <button class="btn" style="margin-top: 10px; background: #6c757d; color: white;" onclick="closeOTPModal()">Cancel</button>
        </div>
    </div>

    <script>
        const assignmentId = <%= assignment.id %>;
        let signatureImageBase64 = '';

        // Handle signature type toggle
        document.querySelectorAll('input[name="signatureType"]').forEach(radio => {
            radio.addEventListener('change', (e) => {
                const textContainer = document.getElementById('text-signature-container');
                const imageContainer = document.getElementById('image-signature-container');
                
                if (e.target.value === 'text') {
                    textContainer.style.display = 'block';
                    imageContainer.style.display = 'none';
                    document.getElementById('signature-text-input').required = true;
                    document.getElementById('signature-image-input').required = false;
                } else {
                    textContainer.style.display = 'none';
                    imageContainer.style.display = 'block';
                    document.getElementById('signature-text-input').required = false;
                    document.getElementById('signature-image-input').required = true;
                }
            });
        });

        // Handle image upload
        document.getElementById('signature-image-input').addEventListener('change', (e) => {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = (event) => {
                    signatureImageBase64 = event.target.result;
                    const preview = document.getElementById('signature-preview');
                    preview.innerHTML = `<img src="${signatureImageBase64}" style="max-width: 200px; border: 1px solid #ddd; border-radius: 5px;">`;
                };
                reader.readAsDataURL(file);
            }
        });

        // Handle form submission
        document.getElementById('approvalForm').addEventListener('submit', async (e) => {
            e.preventDefault();
            
            const remarks = document.getElementById('remarks').value;
            const signatureType = document.querySelector('input[name="signatureType"]:checked').value;
            const signature = signatureType === 'text' 
                ? document.getElementById('signature-text-input').value 
                : signatureImageBase64;

            if (!signature) {
                alert('Please provide a signature');
                return;
            }

            const approveBtn = document.getElementById('approveBtn');
            approveBtn.disabled = true;
            approveBtn.textContent = 'Processing...';

            try {
                const response = await fetch(`/professor/assignments/${assignmentId}/approve`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        remarks,
                        signature,
                        signatureType
                    })
                });

                const data = await response.json();

                if (data.success) {
                    showOTPModal();
                } else {
                    alert(data.message || 'Error processing approval');
                    approveBtn.disabled = false;
                    approveBtn.textContent = '✓ Approve Assignment';
                }
            } catch (error) {
                console.error('Error:', error);
                alert('Error processing approval. Please try again.');
                approveBtn.disabled = false;
                approveBtn.textContent = '✓ Approve Assignment';
            }
        });

        function showOTPModal() {
            document.getElementById('otpModal').classList.add('active');
            document.getElementById('otpInput').value = '';
            document.getElementById('otpInput').focus();
            showOTPMessage('OTP has been sent to your email. Please enter it below.', 'info');
        }

        function closeOTPModal() {
            document.getElementById('otpModal').classList.remove('active');
            document.getElementById('approveBtn').disabled = false;
            document.getElementById('approveBtn').textContent = '✓ Approve Assignment';
        }

        // Handle reject button
        document.getElementById('rejectBtn').addEventListener('click', async () => {
            const remarks = document.getElementById('remarks').value;
            
            if (!remarks || remarks.trim() === '') {
                alert('Please provide remarks explaining why the assignment is being rejected.');
                return;
            }

            if (!confirm('Are you sure you want to reject this assignment? This action cannot be undone.')) {
                return;
            }

            const rejectBtn = document.getElementById('rejectBtn');
            const approveBtn = document.getElementById('approveBtn');
            rejectBtn.disabled = true;
            approveBtn.disabled = true;
            rejectBtn.textContent = 'Processing...';

            // Get signature if provided (optional for rejection)
            const signatureType = document.querySelector('input[name="signatureType"]:checked').value;
            const signature = signatureType === 'text' 
                ? document.getElementById('signature-text-input').value 
                : signatureImageBase64;

            try {
                const response = await fetch(`/professor/assignments/${assignmentId}/reject`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        remarks,
                        signature: signature || '',
                        signatureType: signature ? signatureType : 'text'
                    })
                });

                const data = await response.json();

                if (data.success) {
                    alert('Assignment rejected successfully!');
                    window.location.href = '/professor/assignments';
                } else {
                    alert(data.message || 'Error rejecting assignment');
                    rejectBtn.disabled = false;
                    approveBtn.disabled = false;
                    rejectBtn.textContent = '✗ Reject Assignment';
                }
            } catch (error) {
                console.error('Error:', error);
                alert('Error rejecting assignment. Please try again.');
                rejectBtn.disabled = false;
                approveBtn.disabled = false;
                rejectBtn.textContent = '✗ Reject Assignment';
            }
        });

        function showOTPMessage(message, type) {
            const messageDiv = document.getElementById('otpMessage');
            messageDiv.innerHTML = `<div class="message ${type}">${message}</div>`;
        }

        // Handle OTP verification
        document.getElementById('verifyBtn').addEventListener('click', async () => {
            const otp = document.getElementById('otpInput').value;
            
            if (!otp || otp.length !== 6) {
                showOTPMessage('Please enter a valid 6-digit OTP', 'error');
                return;
            }

            const verifyBtn = document.getElementById('verifyBtn');
            verifyBtn.disabled = true;
            verifyBtn.textContent = 'Verifying...';

            try {
                const response = await fetch(`/professor/assignments/${assignmentId}/verify-otp`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ otp })
                });

                const data = await response.json();

                if (data.success) {
                    showOTPMessage('Assignment approved successfully! Redirecting...', 'success');
                    setTimeout(() => {
                        window.location.href = '/professor/assignments';
                    }, 2000);
                } else {
                    showOTPMessage(data.message || 'Invalid OTP. Please try again.', 'error');
                    verifyBtn.disabled = false;
                    verifyBtn.textContent = 'Verify & Complete Approval';
                }
            } catch (error) {
                console.error('Error:', error);
                showOTPMessage('Error verifying OTP. Please try again.', 'error');
                verifyBtn.disabled = false;
                verifyBtn.textContent = 'Verify & Complete Approval';
            }
        });

        // Allow Enter key to submit OTP
        document.getElementById('otpInput').addEventListener('keypress', (e) => {
            if (e.key === 'Enter') {
                document.getElementById('verifyBtn').click();
            }
        });
    </script>
</body>
</html>
